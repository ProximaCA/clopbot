# Changelog - clopbot v2

## [2.0] - 2026-02-02

### ✅ Реализовано

#### 1. Ребрендинг: nanobot → clopbot
- Обновлена персона в `PERSONA.md`
- Бот теперь представляется как "clopbot" (клопбот)

#### 2. Голосовые сообщения - только по команде
**До**: Автоматически отправлял голосовое, если ответ > 200 символов  
**После**: Отправляет голосовое ТОЛЬКО по явной команде:
- "ответь голосовым"
- "send voice"
- "голосом"

#### 3. Умные триггеры в групповых чатах
Бот НЕ реагирует на все сообщения. Реагирует только на:

**Direct Triggers (100% ответ)**:
- `@cloptbot_bot` (упоминание)
- Сообщения с: "клоп", "бот", "клопбот", "clopbot"

**Context Triggers (20% рандомный ответ)**:
- tonify, ton, nigredo
- web3, degen
- spotify, soundcloud
- и другие контекстные слова из памяти

#### 4. Рандомные реакции на контекст
Если в чате упоминают ключевые темы из памяти бота:
- Бот с вероятностью 20% сам пишет комментарий
- Работает для слов: tonify, ton, nigredo, web3, и т.д.
- Вероятность можно настроить в `telegram.py`

#### 5. Файл триггеров
Создан `~/.nanobot/workspace/TRIGGERS.md`:
- Список всех триггеров
- Можно редактировать
- Применяется после перезапуска бота

### 📝 Конфигурация

**Модель**: `gemini/gemini-2.0-flash-exp`  
**Персона**: сохранена в `~/.nanobot/workspace/PERSONA.md`  
**Память**: сохранена в `~/.nanobot/workspace/memory/MEMORY.md`  
**Триггеры**: сохранены в `~/.nanobot/workspace/TRIGGERS.md`

### 🔧 Технические детали

1. **Триггеры в группах** (`_should_respond_in_group`):
   - Проверка на direct mentions
   - Проверка на keywords
   - Рандомная проверка на context keywords (20%)

2. **Голосовые сообщения**:
   - Удалена автоматическая генерация по длине
   - Только через `metadata.get("voice", False)`

3. **Логирование**:
   - Все сообщения логируются
   - Видно, когда бот игнорирует сообщения ("no_trigger")
   - Видно, когда бот реагирует ("trigger_клоп", "random_context_tonify")

### 🧪 Тестирование

#### В личке (должно работать):
```
привет
кто ты
расскажи про tonify
```

#### В группе (должно работать):
```
@cloptbot_bot привет
привет клоп
кто такой бот?
расскажи про tonify клопбот
```

#### В группе (должно иногда работать ~20%):
```
я запустил tonify
что думаете про nigredo?
новый токен на ton
```

#### В группе (НЕ должно работать):
```
привет всем
как дела?
что нового?
```

### 📚 Файлы изменены

- `nanobot/channels/telegram.py` - добавлены триггеры, убрано автоматическое TTS
- `nanobot/agent/context.py` - добавлена инструкция про голосовые
- `~/.nanobot/workspace/PERSONA.md` - ребрендинг на clopbot
- `~/.nanobot/workspace/TRIGGERS.md` - новый файл с триггерами

### 🎯 Следующие шаги

1. Протестировать в группе с разными триггерами
2. При необходимости добавить больше контекстных слов в `TRIGGERS.md`
3. Настроить вероятность рандомных ответов (сейчас 20%)
